{
  "name": "__PACKAGE_NAME__",
  "private": true,
  "type": "module",
  "version": "0.0.0",
  "description": "HyperBun starter generated by create-hyperbun.",
  "scripts": {
    "dev": "bun run --hot src/server.ts",
    "start": "bun run src/server.ts",
    "build": "bun run build:assets && bun run build:server",
    "build:assets": "bun run scripts/build-assets.ts",
    "build:server": "bun build src/server.ts --target bun --outdir dist --minify --packages external"
  },
  "dependencies": {
    "@hyperbun/core": "__HYPERBUN_VERSION__",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/react": "^19.2.2",
    "@types/react-dom": "^18.3.1",
    "bun-plugin-tailwind": "^0.1.2",
    "tailwindcss": "^4.1.14",
    "typescript": "^5.6.3"
  }
}
